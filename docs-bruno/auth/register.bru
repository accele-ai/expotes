meta {
  name: register
  type: http
  seq: 2
}

post {
  url: {{base}}/auth/register
  body: json
  auth: none
}

body:json {
  {
    "email": "{{email}}",
    "password": "{{password}}""
  }
}

script:post-response {
  bru.setVar("session",res.body.value)
}
